type DBQuery {
  id: Int!
  query: String
  results: String
  hits: Int
  status: Int
  created_at: DateTime
  updated_at: DateTime
}

extend type Query {
  queries: DBQuery @paginate(defaultCount: 10, builder: "App\\Query@resolverQuery")
}
